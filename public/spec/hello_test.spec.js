define([],function(){

    describe("Start test to check if everything is configured properly",() => {
        it("It should return 8",() => {
            expect(4+4).toBe(8);
        });
    });

    describe("Second test function", () => {
        it("It should return true", () => {
            expect(4 < 5).toBe(true);
        })
        }
    );
});